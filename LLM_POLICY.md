# ShuYo LLM 使用政策

本文说明在 ShuYo 开源项目中使用大语言模型（LLM）辅助开发时的基本约定。

## 基本原则

LLM 是辅助工具，不是代码或内容的最终责任人。提交 Issue、Pull Request、代码、文档和数据的贡献者，仍需对其准确性、安全性、许可证兼容性和后续维护负责。

我们希望这份政策能够保护贡献者和用户，同时尽量保持较低的参与门槛。

## 可以使用 LLM 做什么

在确认不包含敏感信息的前提下，LLM 可以用于：

- 讨论实现思路、拆分任务和分析错误信息；
- 编写或改进 Dart、Flutter、Android、iOS 代码；
- 编写测试、测试数据和文档；
- 代码重构、注释和格式整理；

## 代码和内容审查

使用 LLM 生成或修改的代码，在提交前必须由贡献者人工检查：

- 代码是否符合项目现有结构和风格；
- 是否引入不必要的依赖、后门、隐私风险或不安全行为；
- 错误处理、权限、网络请求和本地数据存储是否合理；
- 文档和用户可见文字是否准确。

## 测试要求

Pull Request 仍需满足项目正常的验证要求。一般情况下，提交前应运行：

```shell
flutter analyze
flutter test
```

涉及 iOS、Android、通知、小组件、WebView、登录或网络行为时，**必须**在受影响的平台上进行模拟器或真机验证，并在 Pull Request 中说明验证结果。
