# ShuYo 双端功能优化计划（待审核）

> 状态：P0 已于 2026-08-21 完成；P1 及后续项目仍待审核和实施。

## 1. 本轮原则与范围

- Android 与 iOS 共用的 Flutter 行为同步修改，除非某项能力明确依赖平台原生实现。
- 不实现 iOS 课表 Widget，不实现 App Store 审核演示态。
- 不调整论坛过期证书的临时策略。
- 模拟器验证由开发者手动完成；代码侧仍执行静态检查、自动化测试和双端构建检查（环境具备时）。
- 优先解决用户能直接感知的问题，其次减少论坛/WebVPN 请求量，最后处理需要较大结构调整的长列表性能问题。

## 2. 已确认问题与优化建议

### P0（已完成）：帖子正文图片缩略图会被强制撑满内容宽度

**现状**

`ForumCookedContent` 虽然能解析 HTML 中的图片宽高，但 `_CookedImage` 最终使用父容器完整宽度创建 `AspectRatio`。因此小尺寸图片也会被放大到横向铺满；缺少宽高的图片还会固定占用一块全宽区域。该渲染器同时用于普通楼层、折叠回复、私信正文和 Markdown 预览，所以问题在双端和多个页面中共存。

论坛列表中的单图预览已经有 `90` 至内容宽度的宽度限制，以及 `150` 至 `260` 的高度限制，但与正文图片不是同一套规则。

**建议方案**

- 新增一套共享的“内嵌图片显示尺寸”计算规则，依据图片元数据和当前内容可用宽度等比缩放。
- 建议正文缩略图最小可见边为 `96` 逻辑像素，最大宽度为内容宽度，最大高度先定为 `360` 逻辑像素；不拉伸、不改变宽高比。
- 原图小于最小显示尺寸时允许有限放大到最小值，避免小到无法辨认；正常和大图只缩小，不额外放大。
- 超长图、超宽图在正文中显示受限缩略图，点击后继续使用现有全屏查看器查看完整内容。
- 图片没有宽高元数据时，加载后读取真实尺寸再稳定布局；加载前使用有限占位，失败时不保留全屏宽的大空白。
- 正文默认采用 `BoxFit.contain`，避免元数据缺失时裁掉内容。
- 统一普通楼层、折叠回复、私信和发帖预览的规则；列表多图仍保留三宫格形式。

**验收用例**

- 小方图、普通横图、普通竖图、超长图、超宽图、无宽高图片、加载失败图片。
- 图片均不超出帖子内容区域，不变形；小图清晰可见，大图不撑出异常高度。
- 点击任一缩略图后，全屏查看、左右切图、缩放和保存图片保持可用。
- Android 与 iOS 在相同屏幕宽度下布局规则一致。

### P1：论坛首屏被摘要预热阻塞，并产生逐帖详情请求

**现状**

- 论坛列表会先等待前 6 个主题摘要全部完成，或等待 `1.2` 秒超时，之后才显示整个列表。
- 每个摘要实际上通过 `fetchTopicDetail` 请求完整主题详情，而不是轻量摘要接口。
- 列表设置了较大的预渲染范围，滚动时会继续提前触发更多完整主题请求。
- 这些预取详情还会进入持久化详情缓存，可能挤占用户真正打开过的帖子缓存。

在弱网、校园 WebVPN 或论坛证书链不稳定时，这会表现为首屏长时间转圈、请求过多以及打开帖子时与后台摘要请求竞争。

**建议方案**

- 主题基础信息到达后立即显示列表，不再用前 6 条摘要阻塞整页。
- 摘要区域使用稳定尺寸的逐行骨架/占位，单条摘要完成后单独更新，避免列表整体跳动。
- 限制摘要请求并发数和预取距离，仅为可见项及紧邻项加载。
- 区分“列表摘要缓存”和“用户打开过的完整帖子缓存”，避免摘要预取污染详情缓存淘汰顺序。
- 优先调查 Discourse 列表接口是否能直接返回 excerpt/缩略图；若不能，再保留受控的详情降级方案。

**验收指标**

- 有缓存时论坛列表应立即可滚动；无缓存时列表接口返回后立即出现标题和作者信息。
- 摘要失败不影响其他主题显示，也不会让同一条目无限自动重试。
- 快速滚动时请求数量受控，进入某一帖子时不被大量摘要请求明显拖慢。

### P1：应用启动阶段同时构建并加载所有主标签页

**现状**

主界面使用 `IndexedStack` 保留四个标签页状态，但也会在启动时构建首页、论坛、消息和个人页。由此会同时触发论坛列表、私信列表、论坛摘要、通知红点和个人活动统计等请求。个人统计本身还会请求用户摘要及多类活动数据。

**建议方案**

- 主标签页改为“首次访问时初始化，初始化后保留状态”。
- 首页只启动首页必需的课表摘要、公告摘要和最小登录态检查。
- 论坛、消息、个人统计在用户首次进入对应标签时再加载。
- 红点保留轻量更新，但与页面完整数据加载解耦。
- 对重复的 session、私信和通知请求增加同请求合并，避免定时刷新、页面进入和手动刷新同时发出相同请求。

**验收指标**

- 冷启动不再自动请求未访问标签页的完整列表和个人活动数据。
- 首次进入标签页正常加载；再次切换时保留列表位置、筛选条件和已加载数据。
- 登录、退出、WebVPN 模式切换后，各标签页缓存能够正确失效并重新初始化。

### P1：通知红点和通知页存在请求放大

**现状**

- “全部通知”通过回复、赞、提及 3 个接口合并；前台每 90 秒刷新红点时也会走这套数据获取流程。
- 通知页为了判断通知目标类型，会对最多 12 个主题逐个执行强制详情刷新。
- 打开通知页的四个分类后还可能分别维护和刷新各自的数据。

这会增加论坛请求频率，在 WebVPN 模式下尤其容易放大延迟和失败率。

**建议方案**

- 先用真实响应确认 `/notifications?filter=all` 是否能完整覆盖当前展示需求；能覆盖时改为单接口红点与全部通知数据源。
- 若仍需 3 类活动合并，红点轮询只获取轻量标识或使用 session 中的未读计数，不周期性下载完整活动列表。
- 通知分类优先从一份已加载数据本地筛选；手动下拉刷新时再更新远端。
- 删除为文案分类而强制刷新 12 个主题详情的做法：优先依据通知负载判断，无法判断时显示保守文案，用户打开后再获取详情。
- 为通知和私信刷新增加可取消/可合并的请求状态，避免连续切页造成重复请求。

**验收指标**

- 红点数量在回复、赞、提及和已读场景下保持正确。
- 前台静置时的论坛请求数显著下降，不再周期性产生多组完整通知请求。
- 通知页首屏不等待额外主题详情；点击通知仍能定位到正确楼层。

### P1：错误提示和空状态行为不一致

**现状**

搜索、通知、课表、用户页、反馈和部分设置页直接把 `Exception.toString()` 展示给用户；同一种登录过期、超时、限流或网络失败在不同页面的文案不同。部分空状态（例如空通知）也无法下拉刷新。

**建议方案**

- 建立共享的错误分类与用户文案映射：登录过期、校园网不可达、WebVPN 解析失败、超时、限流、服务端异常和未知错误。
- UI 只展示可操作的简短说明；详细异常保留在调试日志中。
- 统一列表的加载、缓存数据、静默刷新失败、首次加载失败和空数据状态。
- 所有可刷新的空列表仍提供下拉刷新或明确的刷新按钮。

**验收指标**

- 常见网络错误不会显示 Dart 异常类型、堆栈式文字或难以理解的原始响应。
- 有旧数据时刷新失败保留旧内容并轻提示；没有旧数据时显示重试入口。
- 登录过期能够统一引导重新登录，不在多个页面重复弹窗。

### P2：长帖会一次性构建全部楼层和正文内容

**现状**

帖子详情使用 `ListView(children: [...])` 一次性创建所有楼层。每层还会解析 HTML、创建富文本、图片和投票控件。楼层数或图片数较多时，首帧、内存和重建成本会持续增长。

**建议方案**

- 改为惰性 Sliver/List 构建，只创建视口附近的楼层。
- 保留现有楼中楼折叠、目标楼层定位、阅读位置恢复、阅读时长上报和底部回复栏。
- HTML 分段解析结果按 `postId + cooked` 缓存，点赞等局部状态变化不重复解析整篇正文。
- 修正正文链接手势识别器的生命周期，避免长时间浏览和反复重建时累积资源。

**实施说明**

该项收益高但改动风险也高，特别是当前的目标楼层跳转和阅读位置恢复依赖楼层 `GlobalKey`。建议在前两批优化稳定后单独实施，不与图片尺寸调整混在同一提交中。

**验收指标**

- 使用 30、100、300 楼以及多图长帖测试首帧、滚动和内存。
- 通知跳转指定楼层、展开楼中楼、返回后恢复阅读位置仍准确。
- 点赞、投票、删除和回复后只更新必要区域，不导致整页明显闪动。

### P2：论坛搜索只读取第一页

**现状**

帖子搜索固定请求 `/search?...&page=1`，页面没有继续加载能力。结果较多时用户无法看到第一页以后的内容。

**建议方案**

- 为帖子搜索增加分页状态、滚动加载和失败重试。
- 新搜索、切换搜索类型或排序时取消/忽略旧请求，防止旧结果覆盖新条件。
- 用户搜索继续遵循接口上限；若接口支持分页再一并接入。
- 可选增加本地搜索历史，但涉及产品偏好，暂不纳入默认实施范围。

### P2：点击区域和大字体适配需要统一检查

**现状**

论坛筛选、搜索选项等控件视觉高度固定为 `32`，部分帖子操作使用紧凑点击目标。系统字体放大时，固定高度控件可能拥挤；视觉区域和实际点击区域也可能低于 iOS 常见的 44pt 触控建议。

**建议方案**

- 保留紧凑视觉外观，但通过外层约束保证至少约 44 逻辑像素的实际点击区域。
- 检查 100%、130%、160%、200% 字体缩放下的标题、筛选项、底栏、设置项和发帖工具栏。
- 对可能溢出的横向选项允许滚动或合理截断，不通过关闭系统字体缩放规避问题。

### P3：全屏图片浏览的连续体验

**现状**

全屏查看器已有缩放、长图、切图、重试和保存能力，功能较完整；但相邻原图没有明确的受控预取策略，多图帖子快速切换时可能逐张等待。

**建议方案**

- 当前图加载完成后，仅预取前后各一张原图，并限制并发与内存占用。
- 离开页面时停止不必要的预取；失败不弹全局错误，不影响当前图片。
- 该项在正文缩略图和请求调度完成后再做，避免同时改变两层图片缓存行为。

## 3. 建议实施批次

### 第一批：直接体验与低风险请求优化

1. 正文缩略图响应式尺寸规则。
2. 论坛列表取消整页摘要等待，改为逐项稳定占位。
3. 统一第一组图片尺寸/HTML 解析单元测试和 Widget 测试。
4. 统一常见论坛错误文案及可刷新空状态。

建议每一项独立提交，便于分别回归和回退。

### 第二批：启动和通知请求治理

1. 主标签页延迟初始化并保留状态。
2. 相同请求合并、失效和刷新时序整理。
3. 通知红点轻量化、通知分类共用数据、移除主题详情预热。
4. 通过调试计数记录冷启动、静置 5 分钟、进入通知页三个场景的请求数，修改前后对比。

### 第三批：较大结构优化

1. 长帖惰性构建及正文解析缓存。
2. 搜索分页。
3. 点击区域与大字体专项回归。
4. 全屏图片相邻预取。

## 4. 测试与双端同步要求

每批完成后至少执行：

- `flutter analyze`
- `flutter test`
- 新增尺寸计算、HTML 图片元数据、请求去重/失效、分页状态的单元测试。
- 新增关键 Widget 测试：小图不撑满、大图不越界、未知尺寸占位、摘要渐进显示、空状态刷新。
- Android 构建检查；iOS 至少执行无签名/模拟器构建检查（开发环境允许时）。
- 开发者在 iPhone 17 模拟器手动验证；Android 同时回归相同用例。

涉及登录和真实论坛数据的项目还需在校园网直连与 WebVPN 两种模式下各验证一次。模拟器无法充分覆盖的照片保存、图片选择、Cookie 和通知权限仍需最终真机测试。

## 5. 审核时需要确认的决策

1. 是否同意正文缩略图先采用：最小可见边 `96`、最大高度 `360`、最大宽度为内容宽度。数值会集中定义，后续可统一调整。
2. 是否同意论坛列表优先显示标题/作者，摘要随后逐条出现，而不是等待首屏摘要齐全。
3. 是否同意主标签页首次进入时才加载完整数据；通知/私信红点继续保留轻量刷新。
4. 长帖惰性构建是否放在前两批稳定之后单独开展。
5. 搜索历史是否保持不做，只先补搜索分页。

P1 及后续项目通过审核前，不实施对应业务改动。
